const invalidFeature = 'Data is not a valid feature';
const invalidPolygon = 'Data is not a valid polygon';
const hasNoName = 'Data is no name in properties';

module.exports = {
    invalidFeature,
    invalidPolygon,
    hasNoName
};